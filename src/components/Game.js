export default function Game (props) {
    const bestScore = () => {
        // if ()
    }


    return (
        <div className="game-scores-container">
            <span className="game-score-text">Current Score: {props.score}</span>
            <span className="game-score-text">Best Score: {props.game}</span>
        </div>
    )
}